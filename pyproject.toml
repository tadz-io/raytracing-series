[project]
name = "raytracing-in-one-weekend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "plotly>=6.0.1",
    "scipy>=1.15.2",
]
